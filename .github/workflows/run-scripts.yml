name: Run Python Scripts

on:
  push:
    branches:
      - main
  workflow_dispatch: 

jobs:
  run-scripts:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || echo "No dependencies to install"

      - name: Run test2.py
        run: python test2.py

      - name: Run tri_red_black_list.py
        run: python tri_red_black_list.py

      - name : Upload CSV
        run:  |
          git config --global user.name 'github-action[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add ./CVSS_EPSS_Global_List/GlobalList.csv
          git add ./CVSS_EPSS_Global_List/Black_Zone.csv
          git add ./CVSS_EPSS_Global_List/Red_Zone.csv
          git commit -m "Automated file modification"
          git push
